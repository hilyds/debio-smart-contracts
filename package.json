{
  "name": "debio-staking-contracts",
  "version": "0.0.1",
  "description": "DeBio ERC20 staking contracts",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:debio": "npx hardhat run --network debio scripts/deploy-service-request-contract.js",
    "deploy:rinkeby": "npx hardhat run --network rinkeby scripts/deploy-service-request-contract.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "@openzeppelin/contracts": "^4.2.0",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.4.3",
    "hardhat": "^2.5.0"
  },
  "dependencies": {
    "dotenv": "^10.0.0"
  }
}
